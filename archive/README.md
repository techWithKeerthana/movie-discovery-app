# Archive

Retired code kept for reference (the project switched its frontend from React web to React Native / Expo, see `../mobile`).
Nothing here is part of the npm workspaces, is installed, or is built.

- `web-frontend/`  the former Vite + React web app (its Vitest suite is inside).
- `web-e2e/`       the former Playwright real-browser suite for the web app (its mock TMDB now lives in `../tools/mock-tmdb.mjs`).
